let num = 5.5;
num = 5.5 + 2;
num = 5.5 - -1;
num = 5.5 * 2;
num = 5.5 / 2;

let x = 11;
num = x + 1 * 2;
console.log(num);

num = (x + 1) * 2;
console.log(num);

let age = 30;
let dogYears = (age + 1) / 7;
console.log(dogYears);

// Challenge
// create a program that calculates the percent a student got on a test based on questions right versus total questions
let studentScore = 18;
let maxScore = 20;
let percent = (studentScore / maxScore) * 100;
console.log(percent);
